name: balloon_pop_game
description: A mobile-based video game where players pop balloons within a time limit.
version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
     sdk: flutter
  provider: 5.0.0
  flutter_svg: ^0.22.0  # For animated positioning


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/balloon.svg